클래스 또는 인터페이스 이름 뒤에 < > 부호가 붙는다.


멀티 타입 파라미터
제너릭 타입은 두개이상의 멀티 타입 파라미터를 사용할수 잇는데 이경우 각 타입 파라미터를 콤마로 구분한다. 
앞과 뒤가 같을 경우 앞이나 뒤쪽을 생략할 수 있다.


제네릭 메소드
public <타입파라미터> 리턴타입 메소드명(매개변수){

}

와일드 카드 타입
<?> <? extends> <? super> 
	최 하위		최 상위를 사용할수 있다.
	<? extends 상위타입> 상위 타입 클래스 제한
	<? super 하위타입> 하위 타입 클래스 제한
	
제네릭 타입의 상속과 구현

제네릭 타입도 다른 타입과 마찬가지로 부모클래스가 될수 있다. 부모가 제네릭이면 자식도 제네릭이어야 한다.
파라미터는 동일하게 가지고 있어야 한다. 구현이나 상속자식 클래스는 자신이 쓸수 있는 파라미터도 있다. 
추가적으로 파라미터를 가질수 있다. <- 자식